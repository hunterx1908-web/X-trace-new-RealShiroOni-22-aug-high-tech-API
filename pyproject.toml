[project]
name = "leak-v3-api"
version = "1.0.0"
description = "High-Tech Leak V3 API"
requires-python = ">=3.9"
dependencies = [
    "Flask==2.3.3",
    "requests==2.31.0",
    "gunicorn==21.2.0"
]

[tool.vercel]
entrypoint = "api:app"